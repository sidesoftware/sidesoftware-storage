name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore
        run: dotnet restore SideSoftware.Storage.slnx

      - name: Build
        run: dotnet build SideSoftware.Storage.slnx --no-restore

      - name: Test
        run: dotnet test SideSoftware.Storage.slnx --no-build
